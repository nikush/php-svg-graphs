PHP SVG Graphs
===

A small library for rendering simple SVG graphs in PHP.

The graphing capabilities are relatively simple, however the SVG drawing class
is very flexible and can be used on it's own to draw almost anything.
